# dhow web — copy to .env. All vars are build-time (VITE_ prefix); never secrets.

# API base. In dev the Vite proxy forwards /api -> http://localhost:4000
VITE_API_URL=/api

# USDC token address on the target chain (must match the deployed FinancingPool asset)
VITE_USDC_ADDRESS=
