{
  "name": "@dhow/underwriting",
  "version": "0.0.0",
  "private": true,
  "description": "Deterministic, explainable cash-flow underwriting engine (dual scorecard -> PD -> expected-loss pricing).",
  "license": "UNLICENSED",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "tsx test/engine.test.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.1",
    "typescript": "^5.6.3"
  }
}
